"""Engines. Import submodules directly (``from world_model.core import lewm``);
this package deliberately does not import them eagerly so that a consumer
without torch can still import ``world_model`` and its contracts."""
