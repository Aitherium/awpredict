"""Engines. Import submodules directly (``from awpredict.core import lewm``);
this package deliberately does not import them eagerly so that a consumer
without torch can still import ``awpredict`` and its contracts."""
